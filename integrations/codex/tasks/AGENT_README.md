# atelier/integrations/codex/tasks

Reusable task markdown for Codex sessions.

- `preflight.md` standardizes context + plan-check + rubric-gate flow.
- `review-repair.md` standardizes rescue workflow after repeated failures.

These files are copied to workspace `.codex/tasks/` by `scripts/install_codex.sh`.
